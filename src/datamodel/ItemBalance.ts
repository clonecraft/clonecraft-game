export default interface ItemBalance {
    collectionId: string;
    itemId: string;
    itemIdNumber: number;

    platform: string;
    userId: string;

    balance: string;
    balanceNumber: number;
}